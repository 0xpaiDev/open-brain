# Superpowers Specs

New specs go in `active/`. Completed specs live in `done/`. See `INDEX.md` for the history of shipped features.

The brainstorming skill defaults to writing specs to `docs/superpowers/specs/` — always write to `docs/superpowers/specs/active/<YYYY-MM-DD>-<topic>-design.md` instead.
